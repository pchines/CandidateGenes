%h1 Listing Users

%table
  %tr
    %th{:colspan => 3}
    %th Username
    %th Full name

  - @users.each do |user|
    %tr
      %td= link_to image_tag('view-icon.png', :alt => 'Show'), user
      %td= link_to image_tag('edit-icon.png', :alt => 'Edit'), edit_user_path(user)
      %td= link_to image_tag('delete-icon.png', :alt => 'Destroy'), user, :confirm => 'Are you sure?', :method => :delete
      %td= user.username
      %td= user.fullname

%br

= link_to 'New User', new_user_path
